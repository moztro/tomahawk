version: '3'

services:
  tomahawk.service.products:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"

  tomahawk.client.angular:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"

